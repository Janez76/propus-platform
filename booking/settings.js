const {
  getSetting,
  getSettingsMap,
  listEffectiveDefaults,
  setSystemSettings,
  seedDefaultSettings,
} = require("./settings-resolver");

module.exports = {
  getSetting,
  getSettingsMap,
  listEffectiveDefaults,
  setSystemSettings,
  seedDefaultSettings,
};
